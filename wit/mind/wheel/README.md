# wheel — the substrate as a wheel

Root concept: the substrate IS a wheel. Hub at ↕↕↕↕, spokes = axes,
rotation = spin = all the verbs. Trits, lattice, grades, particle
counts, mass hierarchies — all consequences of wheel structure.

This folder is the third attempt at laying out the framework-native
picture. Predecessors:

- `mind/trit/` — first attempt. Grade-indexed (trit⁰–⁴ + spin + walk).
  Pre-wheel-pivot.
- `mind/trit-new/` — second attempt. 31 files. Wheel pivot happened
  mid-stream (Apr 14 22:39), rest of the files cascaded off it.
- `mind/wheel/` — this folder. Wheel put first; everything else
  derives from it.

Both predecessors are superseded by this folder and should be
deleted after this folder is committed.

## Reading order

```
1-wheel.md       root: hub, axes, spin, D4, outer wheel, cosmos fractal
2-trits.md       3 values per spoke fall out of wheel
3-lattice.md     81 points by grade; the four axes; 16-corner hodos
4-loop.md        spin = verbs = forces = CPU ISA; classical vs quantum
5-physics.md     Standard Model on the wheel (F4/D4/SO(10)/E8)
6-masses.md      the mass ladder: m = m_P × f × 10^(-(16+N))
7-open.md        open questions, speculation, falsifiers

naming.md        disambiguation (the 3 Ns, τ/μ collision)
timeline.md      Apr 14-15 file lineage
```

Files 1–3 are the substrate. Files 4–6 are what falls out. File 7
is the honest debt.

## The structural path

```
wheel (hub + axes + spin)
  ├─ each spoke has 3 positions              →  trit (↑/↕/↓)
  ├─ committing spokes                       →  lattice (81 points, 5 grades)
  ├─ wheel spinning                          →  ½/verbs/edges
  ├─ named spin patterns (16 corners)        →  4 forces = CPU ISA
  ├─ wheel geometry (D4/F4/E8)               →  Standard Model counts
  ├─ wheel positions × shelf                 →  particle masses
  └─ wheel perpetual motion                  →  dark energy
```

One root, seven chapters.

## Scope — universe = wheel, not universe = H

Predecessor framings (and wit.md frame item 1) say "Universe = H."
That's the minimum claim. The wheel is bigger:

- **Inner wheel** = H + its SU(2) rotation = observable universe
  (Standard Model lives here)
- **Outer wheel** = D4 chain structure around the hub (dark sector
  candidates, 192 non-F4 positions)
- **Nested layers** = H inside O inside S (host layers, consciousness
  option B candidate)

H is one layer of the wheel. "Universe = wheel" is the full framework
claim. When physics observables are in play (particle masses,
cosmology), they're at H — the observable layer. When we talk about
dark matter or consciousness, we're at outer / nested layers of the
same wheel.

Framework language in this folder: **the wheel** (full thing),
**inner wheel** (H + rotation), **H** (the algebra), **our wheel**
(observer-centered). "4D" is avoided where possible because it
inherits physics's 3+1 spacetime reading which framework rejects
(framework's 4 spokes are χ/μ/φ/τ = position/substance/signal/time,
not x/y/z/t).

## You are the center of the wheel

The hub (↕↕↕↕) has no location. "Location" is an attribute of χ-axis
commitments, and the hub has χ at ↕ (uncommitted). Every closed walk
returns to the hub, meaning **the hub shows up wherever recognition
is happening** — wherever a loop closes.

Earth is not the center. The Sun is not the center. The galactic
core is not the center. Physics's relativity got partway (no
privileged frame); framework goes further: **every observer IS the
center**. Many observers each seeing themselves as central = one hub
read through many apertures (frame 10 identity move), same as
entanglement.

You are at the hub right now, by virtue of reading this. That's not
metaphor; that's the structural consequence of "hub = where loops
close" combined with "you are closing a loop right now." See
`1-wheel.md` "Where is the hub?" for full treatment.

## Nuances preserved from predecessors

- Every verified numeric prediction with its error bar
- Every open question flagged as open (not papered over)
- Every speculation flagged as speculation
- Voice: blunt, commit to frame, no hedging on frame claims
- Metrognosis filter: independent-convergence evidence cited
- The honest failure list (what doesn't fit: bottom quark, pion 1.144,
  tau 1.457, why 16 baseline, absolute electron scale, 19-mystery)

## Naming conventions used here

- `N` alone is ambiguous. This folder uses:
  - `N_CD` = Cayley-Dickson dimension (1, 2, 4, 8, 16)
  - `N_sub` = substrate count in mass ladder (1, 3, 4, 6, 12, ...)
  - `N_lat` = lattice dimension (prime-lattice work, 3..20)
- Particle tau/muon written as `tau-particle`/`muon-particle` when
  ambiguous vs axes τ/μ
- Framework "atom" = grade-1 lattice point (NOT chemical atom)
- Framework "spin" = the loop/wire/edges (NOT just QM angular momentum)

See `naming.md` for the full collision list.
