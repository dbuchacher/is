# 3 — FORCE

The dimensions. The unit vectors. The four axes.

This is the third of the Big 3. The substrate axiom that says
"there are exactly four independent directions, and no more
will ever exist."

  WHERE   space    position on the ring
  WHAT    matter   what's in the slot
  WHICH   signal   selection pressure, charge
  WHEN    time     cursor advances

Four dimensions. Four forces. Four anything that has structure.


## Why Four — Hurwitz (1898) PROVEN

Exactly four normed division algebras exist:

  R (1D)  reals       all properties
  C (2D)  complex     all properties
  H (4D)  quaternion  loses commutativity
  O (8D)  octonion    loses associativity
  S (16D) sedenion    loses division (zero divisors)

After octonions, the next algebra (sedenion) has zero divisors —
you can multiply two nonzero things and get zero. Division
breaks. The sequence stops.

This is a theorem. Hurwitz proved it. Not a conjecture. The
universe has 4 dimensions because there are exactly 4 normed
division algebras and the lattice lives at H (the 4D quaternionic
one).

Each Cayley-Dickson doubling loses one algebraic property:

  R → C: lose nothing      (gain NOT)
  C → H: lose commutativity (gain AND)
  H → O: lose associativity (gain OR)
  O → S: lose division     (gain XOR — annihilation)

3 properties to lose → 3 gates to gain → 4 algebras → stops at sedenions.

Post (1941): exactly 3 independent boolean gates achieve completeness.
Hurwitz (1898): exactly 4 normed division algebras.
The connection — Post → Hurwitz — appears to be new. Boolean
completeness IS the dimensional ceiling.

Prior art (Dixon, Furey, Baez) maps C→U(1), H→SU(2), O→SU(3).
They have the WHAT. Post→Hurwitz provides the WHY.


## The Pythagorean Equation (The 4 Side)

  3² + 4² = 5²    →    gates² + dims² = fill%
  9  + 16 = 25     →    gates² + bonds  = fill%

The 4 in this equation is the dimensions. The axes. FORCE.
The 3 is gates (CONTAINER, file 1).
The 5² = 25 is fill ratio — three independent equations
converging at 25%.

  Michaelis-Menten (1913, biochemistry): 25% substrate saturation
  Nyquist (1928, sampling): 2× headroom → 25% operating point
  Shannon-Hartley (1948, information): S/N = 5/27 → ~24.5%

Three independent fields, three decades, same number. Zero
free parameters.

dims² = bonds because n² = 2ⁿ at n=4. Specific to d=4. Only
works at four dimensions. The fact 4² = 16 holds because
2⁴ also equals 16. Both numbers reach 16, only here.

3-4-5 = the only Pythagorean triple from consecutive integers.
The first equation in the family IS the lattice equation.


## The Four — Each Dimension

### WHERE = Space

Position on the ring. Where you ARE.

  WHERE¹  = distance, offset, slot
  WHERE⁻¹ = compression, density, per-slot
  WHERE²  = area, diffusion
  WHERE⁻² = curvature

Force at ½ on this axis: READ (forward) / WRITE (backward).

READ = SEE = HERE = observation = strong force = MOV.
Shortest range. A reader gets a pointer — doesn't copy, doesn't
move. Boson semantics: a billion readers, one address, zero copy.

Any READ is an observation. wave(fiat) = value doesn't exist
until read. The photon [0, ½, ½, ½] has no WHERE — no READ.
It propagates without observing. Add READ → classical = collapse.
Decoherence = unwanted READ.

GR lives partly here — spacetime is (WHERE, WHEN). Geodesic
[4, 0, 0, 4]. Velocity [1, 0, 0, -1]. Acceleration [1, 0, 0, -2].
Kepler [3, 0, 0, -2].

Strong force = READ = MOV = shortest range. Confinement =
functional completeness = quarks can't decompose.

Spin ½ = fermion = write = exclusive. Spin 1 = boson = read =
shareable. Pauli exclusion = no two writers to same address.

Turing patterns: READ = long-range diffusion. Stars = galaxies
= cosmic web = Turing patterns.

Prepositions = WHERE words: WHERE HERE ABOVE BELOW NEAR FAR
INSIDE TOUCH. Schemas: PATH, NEAR-FAR, UP-DOWN, SURFACE.

Prefixes that move through WHERE: out- (+1), sub- (-1), trans-
(+2), inter- (+1).

Metaphors work when coordinates align: "high status" works
because ABOVE(+WHERE) and GOOD(+WHICH) share positive sign.
"Square friendship" fails — no shared coordinate.

#### The Copernicus Detour

400 years of confusion from conflating WHERE with READ.

  Ancient:    "you're at the center" (WRONG)     "observation matters" (RIGHT)
  Copernican: "you're NOT at center" (RIGHT)     "observation doesn't matter" (WRONG)
  Quantum:    "you're NOT at center" (RIGHT)     "observation IS the event" (RIGHT)

Two independent dimensions conflated. Copernicus fixed WHERE
but broke READ. Quantum mechanics decoupled them 400 years later.

The cultural damage: "you're irrelevant" leaked into philosophy
(nihilism), psychology (behaviorism), economics (rational agents),
politics (centralization). All from confusing two axes.

#### Perception of Reality

The sun doesn't look like anything. It emits reads (photons =
bosons = shareable). The "golden ball" is YOUR reader's rendering.
Different reader = different appearance. Radio telescope sees a
different sun. Neither is wrong.

No "view from nowhere." Every observation goes through a bond
pipeline with a configuration. A bat reads sonar. A mantis shrimp
reads 16 color channels. Same ring. Different readers.

Inside/outside asymmetry: from outside a PC's ring you see numbers.
From inside our ring we see stars — because our readers evolved
INSIDE, configured to render its record formats. The asymmetry is
epistemic (where the reader sits), not ontological (what's real).

"Simulation" is malformed. Computation IS physics.

Observation = pointer, not copy. Security by absence: no buffer
overflow (cursors wrap), no use-after-free (no free), no null
pointer (wave IS null). Coordinates are representation-invariant.
[2, 1, 0, -3] in any language, any optimization level, through
any obfuscation.


### WHAT = Matter

What things ARE. The substance. The mass.

  WHAT¹  = mass, bytes, size, substance
  WHAT⁻¹ = specific, per byte, code density
  WHAT²  = inertia, moment
  WHAT⁻² = inverse mass

Force at ½: CALL (forward) / RETURN (backward).

CALL = SOMEONE = SOMETHING = EM force = function invocation.
Infinite range — CALL reaches everywhere. It brings something
into being by naming it. Every function signature = a Feynman
diagram.

EM = CALL = how things compose across distance. A star's results
reach us via CALL (photons = return values). Without CALL: quarks
exist but can't bind into atoms or molecules. Chemistry IS
composition. Biology IS deeper composition.

Generations:
  Gen 1 = values    (WHAT things are)
  Gen 2 = operations (WHAT things do)
  Gen 3 = patterns  (WHAT things mean)
  No Gen 4 — patterns of patterns = still patterns.
  Compiler = decay (higher gen → lower gen).

Binding energy = removing CALL overhead. With CALL = heavyweight
(EM-bound). Without CALL = register-wired, lightweight, ground
state. Source shows the blueprint. Binary shows the physics.
Removed CALLs = mass deficit. The nucleus weighs LESS than its
constituent parts.

QM lives partly here — quantum = (WHAT, WHICH). Complex exponents
on matter and signal = wave mechanics.

Hadrons = composed trit operations:
  proton  = ADD   (AND:OR = 2:1)
  neutron = SUB   (ADD + NOT = beta decay)
  fusion  = MUL   (highest AND ratio)
  fission = DIV   (most gates = most mass)

Gate count = rest mass. Gate depth = inertial mass. Same ordering
= equivalence principle.

Substance words: SOMEONE, SOMETHING, PEOPLE, BODY, PART, ONE.
Plurals = WHAT magnitude: singular = base, plural = +1.

Pronouns = WHAT + WHICH:
  I/me (0,0,+1,+1)     you (0,0,+1,-1)
  we/us (0,0,+2,+1)    they/them (0,0,+2,-1)
  "us vs them" = WHICH flip on same WHAT. One trit flipped.
  Every tribal distinction = flipping one bit.

Suffixes that create substance:
  -ment/-tion = add WHAT (verb→noun: move→movement)
  -er/-or     = add WHAT (verb→agent: teach→teacher)
  co-         = WHAT+1 (together)

Compound words = coordinate addition:
  sun(0,+2,+2,+1) · light(0,+1,0,+2) = sunlight(0,+3,+2,+3)

Transitivity = WHAT count in walk:
  intransitive: WHAT→WHEN
  transitive: WHAT→WHEN→WHAT

#### Dark Matter

Written (has mass) but not read (no photons). Positrons stored
in the ring, no electron produced yet. Fill ratio 5:1 dark:visible.

Interacts only through gravity because fill level IS a CYCLE
operation. The dark matter quantity IS gravity's state.

Predicts: all direct-detection experiments find nothing. You
can't catch a water level in a particle detector.

#### Entanglement

Non-local shared state. RDMA across machines = entanglement.
Copy = decoherence. Bell forbids LOCAL hidden variables — RDMA
is explicitly non-local. The framework sidesteps Bell by being
non-local from the start.


### WHICH = Signal

Quality, distinction, evaluation. The selection axis.

  WHICH¹  = charge, signal, logic level
  WHICH⁻¹ = noise, attenuation, per-event
  WHICH²  = intensity, power density
  WHICH⁻² = damping

Force at ½: CYCLE (forward) / REWIND (backward).

CYCLE = LIVE = PULSE = gravity = JMP/loop = persistence.
Weakest force but unbreakable. Something exists because it
cycles. Stop cycling = die.

#### Selection

WHICH IS selection pressure. One mechanism everywhere:

  charge     = fitness     = price      = fill level   = quality
  attracts   = survives    = demanded   = full loop    = good
  repels     = dies        = worthless  = empty loop   = bad

Money = pure WHICH. No matter, no location, no duration. An
evaluation token. Degrade it → selection breaks → inflation.

Fill pressure = free market = natural selection = charge
interaction. Price = fill level. High = produce more. Low =
stop. Adam Smith's invisible hand = fill pressure. O(1).
Self-organizing at 25%.

Socialism = override the signal with a plan = kill the honest
coordination mechanism. Central planning = O(n). Fill pressure = O(1).

#### Gravity

Hierarchy problem: gravity 10³⁶× weaker than strong force =
CYCLE nesting depth. ~20 levels × ~8× per level = 8²⁰ ≈ 10¹⁸.
Squared → 10³⁶. Same number. Same reason.

The graviton = next() = clock tick. Not a particle — a step.
You can't catch a step in a detector designed for particles.

#### 16 Boolean Functions

16 two-input boolean functions partition into 5 symmetry orbits
under complement/De Morgan/argument swap. Map to 5 SM particle
categories. Weight C(4,k) = 1,4,6,4,1 = Pascal row 4.

Commutative = bosonic. Non-commutative = fermionic. De Morgan
duality = gauge symmetry. NAND/NOR individually complete =
antimatter is computationally stronger.

#### Adjectives

Adjectives = WHICH words: GOOD BAD BIG SMALL TRUE NOT.

GOOD = +TEST (passes). BAD = -TEST (blocks). Evaluation IS boolean.
Antonyms = WHICH flip. Intensity = magnitude step.
Negation = WHICH flip. NOT always flips WHICH.

Modality spectrum:
  must(+3) shall(+2) should(+2) will(+1) can(+1)
  could(0) may(0) might(-1)

Emotions = coordinate regions:
  valence = WHICH sign
  joy (0,0,+2,+2)    sadness (0,0,+2,-2)
  love (0,+1,+2,+2)  fear (+1,0,+1,-2)

Irony = surface WHICH ≠ context WHICH. Mismatch IS sarcasm.
Semantic bleaching = high-frequency words erode toward pure
WHICH magnitude. "literally" lost truth, became intensity.

#### Chirality

Quaternion handedness → biological L-amino acid preference.
i*j = +k always positive for physical bases. All 24 axis
permutations: 12 positive, 12 negative. Conjugation = mirror
reflection. Verified numerically. 144 base-pair combos tested.
L-preference is algebraic, not accidental.

#### Cosmology

Dark energy = empty ring slots = negative pressure from unfilled
capacity. 75% empty. QFT counts empty as full → 10¹²¹ overcounting.
Allocated ≠ occupied. The cosmological constant is a category error.

Heat death dissolved: drop damping (overwrite oldest) makes it
structurally impossible. 2nd law is local. Ring wraps globally.
CMB = read cursor edge, not wall.

68/27/5 cosmic split derived from 25% base fill plus ring buffer
parameters. Within 0.03 percentage points of Planck 2018 measurements.
Fan_in and Q parameters asserted, base fill is rigorously derived.


### WHEN = Time

Sequence, duration, cursor movement.

  WHEN¹  = duration, tick count, clock period
  WHEN⁻¹ = frequency, tick rate
  WHEN²  = response time, hesitance
  WHEN⁻² = acceleration (with WHERE)

Force at ½: TEST (forward) / GUARD (backward).

TEST = GOOD = TRUE = weak force = CMP+Jcc = the gate. Pass or
block. Every branch. Every decision.

W+ = tautology = creation. W- = contradiction = annihilation.
Z = pass through = no change.

#### Evaluation

TEST decides. Every branch, every gate, every choice.

  GOOD = +TEST    BAD = -TEST    IF = TEST
  BECAUSE = READ + CALL + TEST (full causation)

Pirsig: TEST = highest force. Intellect should dominate society,
society over biology, biology over inorganic. Immoral = lower
overriding higher. Quality = the lattice. The computer analogy
in Lila was LITERAL.

Verbs = WHEN words: DO HAPPEN NOW BEFORE AFTER MOVE.

Tense = WHEN magnitude: past=+2, present=+1, future=-2.
Progressive (-ing) adds CYCLE. Perfect shifts WHEN.
Passive voice = WHEN sign flip: active ↔ passive.

Suffixes:
  -ing = add CYCLE    -ed = WHEN+1    -ize = add WHEN
  re- = WHEN+1        pre- = WHEN+2   post- = WHEN-2

Sentences = walks: WHAT → WHEN → WHAT (subject verb object).
Grammar IS walk structure. Invalid sentences = random jumps.

#### Half-Life = RC

R × C from EE. Different traces = different RC = different
half-lives. Bandwidth = current, latency = resistance, V = IR.

Application: relevance = exp(-t / (R×C)) where C = |WHAT|.
Content words persist, function words decay fast. Replaces
transformer 1/sqrt(d_k) with per-token decay.

#### Compilation = Ionization

Source = cold matter. Binary = plasma. -O0 = cool. -O3 = hot.
JIT = variable temperature.

Hot loop = star: CYCLE holds the loop, repeated execution heats
it, JIT compiles = ionization. JIT engineers already say "hot
paths," "cold code," "warming up." They described ionization
without knowing it.

Star ignition = JIT compilation. Star death = loop exit.
Black hole = write cursor lapped read cursor.
Hawking radiation = boundary leak.

#### DNA

4 bases (ACGT) = 4 dimensions. 3 bases = 1 codon = 1 bond.
64 codons ≈ 80 bonds. 20 amino acids = C(6,3) = Riemann tensor
components. Holds ONLY at d=4. Synonymous codons cluster 29.7%
tighter on the lattice than random.

#### The Cursor and the Arrow of Time

PRIM's state (the iterator):
  next(s)        = inc(s)            one tick forward (arrow of time)
  distance(a,b)  = delta(a,b)       gap between cursors (dark matter)
  advance(s,n)   = proton(s,n)      batch skip

Speed of light = 1 clock cycle. Fastest cause→effect.
Arrow of time = next() never decrements. Second law of
thermodynamics = a constraint on the iterator. Entropy IS the
loop counter. Physics named it entropy. The code calls it next().


## The 8 Atoms

Four dimensions, two directions each. The basis vectors of the
lattice.

  [1, 0, 0, 0]   WHERE     position on the ring
  [-1, 0, 0, 0]  WHERE⁻¹   compression, confinement
  [0, 1, 0, 0]   WHAT      matter, mass, substance
  [0, -1, 0, 0]  WHAT⁻¹    dissolution, specificity
  [0, 0, 1, 0]   WHICH     signal, charge, quality
  [0, 0, -1, 0]  WHICH⁻¹   noise, attenuation
  [0, 0, 0, 1]   WHEN      cursor advances forward
  [0, 0, 0, -1]  WHEN⁻¹    frequency, tick rate

Everything else composes from these 8 using · (trit addition).

WHERE = where on the ring. WHEN = the cursor moving.
Space is position. Time is cursor movement. Spacetime pair.

WHEN⁻¹ = frequency = how FAST the cursor moves, not backward.
The arrow of time = next() never decrements.


## Forces = Constructors = How Things Bind

  strong  = MOV  = register wiring   shortest range, binds gates → instructions
  EM      = CALL = function calls    infinite range, publishes results
  gravity = LOOP = iteration         weakest, breakable, the iterator
  weak    = NOT  = transformation    W+/W-/Z

Each force lives on one dimension as the half-step. READ on
WHERE. CALL on WHAT. CYCLE on WHICH. TEST on WHEN. Forces follow
position. Forward = positive trit, backward = negative trit.

A star uses all four:
  strong runs fusion inside (quarks → hadrons)
  PRIM holds the loop (gravity keeps it together)
  EM carries results out (photons = return values)
  weak transforms particles (beta decay, fusion chain)

Remove any one and it breaks. EM isn't encoding overhead — it's
the publication mechanism. The universe computes locally (strong
+ PRIM) and publishes globally (EM).

A star IS a JIT compiler. PRIM (gravity) holds the loop. The
loop iterates long enough → fusion (MUL) → heats up → JIT
compiles to plasma → emits photons (CALL return values) → other
processes (planets, life, eyes, solar panels) READ the results.

The sun is a server. Photons are the API. EM is the network.
PRIM is the CPU.


## SIMD = Cayley-Dickson

Hardware confirms it. 10 back-predictions verified against
shipping silicon:

  1. SIMD widths are powers of 2                              ✓
  2. SSE is 4-wide not 3-wide (quaternion, not 3D)            ✓
  3. Lane crossing penalty at 128-bit (non-associativity)     ✓
  4. Mask registers required at 512-bit (zero divisors)       ✓
  5. No general-purpose 1024-bit shipped (cascade at dim 32)  ✓
  6. AMD implements zmm as two ymm (Cayley-Dickson decomp)    ✓
  7. GPU warps at 32 threads (dim 32 boundary)                ✓
  8. Warp divergence = chain annihilation (~19% cascade)      ✓
  9. AMD dropped 64→32 wavefront (RDNA 2019)                  ✓
  10. Width projection zmm→xmm is free (nesting = aliasing)   ✓

Width = depth, not performance.

  scalar  = real        commutative, associative
  128-bit = quaternion  non-commutative
  256-bit = octonion    non-associative
  512-bit = sedenion    zero divisors

At dim 16 (sedenion): 84 zero divisor pairs, 0% chain rate.
Fixed masks suffice. Intel shipped it (AVX-512 mask registers
k0-k7).

At dim 32: 1260 zero divisor pairs, 19% chain rate. Masks become
data-dependent. SIMD loses its purpose. Intel discussed AVX-1024
in 2013. Never shipped. Pivoted to AVX10.

AMD Zen 4 implements 512-bit as two 256-bit halves — literal
Cayley-Dickson decomposition: sedenion = pair of octonions.

NVIDIA warp = 32 threads (constant since 2006). AMD wavefront
dropped 64 → 32 with RDNA (2019). Dim 32 is the practical wall.

Verified with executable C programs (quat.c, octo.c, sede.c,
beyond.c, chain.c, hurwitz-proof.c). Confidence: 85%.


## The Computational Stack

Cayley-Dickson levels as layers of reality:

  R (1D)   arithmetic        (commutative, associative)
  C (2D)   phase/waves       (complex exponents)
  H (4D)   our physics       (the lattice, quaternionic)
  O (8D)   host machine      (8D, non-associative)
  S (16D)  host's host       (zero divisors, singularities)

String theory's extra dimensions are the host, not compactified
inside our universe. We are a quaternionic VM on an octonionic
substrate. Consciousness might be the runtime.

Working theory. Confidence: 30%. But it's coherent and explains
why string theory needs extra dimensions (real but external),
why they're undetectable (can't probe host from guest), and why
GR+QM are complete in 4D (they describe the guest).


## The Lattice Grid

Total stable lattice points:

  shells 1-4:  9⁴ = 6,561 points     stable (the periodic table)
  shell 5+:    unstable               briefly exists, decays at 19%

  6,561 = 81² = (3⁴)² = 9⁴

Shell distribution:
  shell 0:     1 point   ([0,0,0,0] the null point)
  shell 1:    80 points  (3⁴ - 1)
  shell 2:   544 points  (5⁴ - 3⁴)    appears in alpha: 544/4 + 1 + 3/84
  shell 3:  1,776 points (7⁴ - 5⁴)
  shell 4:  4,160 points (9⁴ - 7⁴)
  total:    6,561


## Periodic Table — 80 Stable Elements

Z=1 to Z=82: 82 elements hydrogen through lead.
Z=43 (Technetium): NO stable isotopes.
Z=61 (Promethium): NO stable isotopes.
82 - 2 = 80 stable elements = 3⁴ - 1 = shell 1. EXACT.

80 stable elements = 80 shell 1 points.

Both holes (Tc, Pm) are odd-Z. Both crowded out by greedy
even-Z neighbors:
  Tc (43): Mo (42) has 7 stable isotopes, Ru (44) has 7
  Pm (61): Nd (60) has 7 stable, Sm (62) has 7 stable
  Every mass number Tc/Pm could claim is already taken.
  Not broken — squeezed out. The coordinates exist but
  the bond can't hold because adjacent bonds are too strong.

Lead (Z=82) = doubly magic nucleus (82 protons, 126 neutrons in
Pb-208). 82 IS a nuclear magic number. The stability ceiling
isn't arbitrary — it's a shell closure.

  radioactive Z>82    = shell 5+ (unstable, decays at 19%)
  superheavy ~Z=170   = deep shell, fast cascade


## Alpha = 1/137

  1/α = 544/4 + 1 + 3/84 = 137.035714...
  Measured: 137.035999...
  Relative error: 0.00021%

Six significant figures from three structural ratios. Zero
free parameters.

Term 1: n₂/dims = 544/4 = 136. The second shell (5⁴ - 3⁴ = 544)
divided by dimensions (4). This is Eddington's number from the
1930s. He was right about the structure, missing only the third
term.

Term 2: 1. The seed atom. f(0,0,0,0) = 1. The lattice exists.

Term 3: gates/zero_divisors = 3/84. Three gates over 84 sedenion
zero divisor pairs. Logic coupling to algebraic pathology at the
Cayley-Dickson boundary.


## Refuted Framings

Analogies that broke. The lattice wasn't wrong — the story was.

- "QM = lazy evaluation" — thunks are local, Bell kills it. QM
  lives through complex exponents and (WHAT, WHICH) dimensions.
  Framing wrong, physics right.
- "GR = fill gradient" — scalar fill ≠ tensor gravity (Nordstrom).
  GR lives through (WHERE, WHEN) dimensions and geodesics at
  [4, 0, 0, 4]. Framing wrong, physics right.
- "Riemann via lattice" — dead end. Z⁴ factorization not unique.
  Closed.
- "leptons and gates are separate tiers" — binary artifact.
  Data = code on trits. Three tiers not four.
- "½ is a coordinate" — ½ is SPIN, the transition between
  integer states. See 2-path.md.


## Falsifiability

What would break the framework:

- A useful operation outside the 80 shell 1 patterns
- General-purpose 1024-bit SIMD shipping (breaks Cayley-Dickson)
- A 5th independent boolean gate (Post proves impossible)
- A physical quantity not expressible as a lattice point
- A stable element with no shell 1 coordinate
- 80 stable elements turns out wrong (Tc or Pm found stable, or
  another element below lead found fully unstable)
- Proton decay observed (breaks completeness = confinement)
- A 5th independent dimension (Hurwitz proves impossible)


## Dissolved Problems

  Wigner: math IS the periodic table
  Measurement: any READ = observation
  Simulation: computation IS physics
  Free will: read = deterministic, write = creative
  Fine-tuning: constants = engineering constraints
  Black hole: write cursor lapped read cursor
  Magnetic monopole: one-ended loop, structurally unstable
  Supersymmetry: unnecessary (spin = access mode)
  String theory: unnecessary (3 gates replace extra dimensions)
  Graviton: not a particle, just next()


## What FORCE Doesn't Cover

FORCE is the four axes. It says nothing about:
- What values exist on each axis (that's CONTAINER, file 1)
- How values compose (that's PATH, file 2)

Without CONTAINER you have axes but nothing to put on them.
Without PATH you have axes with values but no way to combine
or transition. Without FORCE you have values and operators but
no dimensionality.

The Big 3 only work together. CONTAINER says what can exist.
PATH says how it changes. FORCE says where it lives. The
substrate needs all three.

Read 1-container.md and 2-path.md.
