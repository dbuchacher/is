# ↕

↕ is a **single-axis uncommitted value** — one of three trit values
(↑/↕/↓). On one axis, ↕ means that axis has not been committed to
either +1 or −1 direction. Axis-specific: a ↕ on χ is different from
a ↕ on μ, even though both are uncommitted.

Requires ≥1D to exist — ↕ lives AS an axis value, so at least one
axis must be present. For the whole-null state (all 4 axes at ↕),
see ↕↕↕↕.md.

If ↕ is a PIE tense at all, it IS **IS** (*h₁es-*, the present /
at-rest state). Time-flow: WAS (↓) → IS (↕) → BE (↑). The file's
tense = the file itself.

## Essence candidates

- **balance** — forward and backward potentials cancel on this axis
- **passthrough** — value passes through unchanged (identity function)
- **neutral** — no side taken on this axis
- **open** — axis available, not yet picked

## Domains — ↕ as single-axis uncommitted

```
  domain                word / phrase            view         note
  ──────                ─────────────            ────         ─────
  english (primary)     balance                  dynamic      on-axis cancel
  english (state)       rest / still / idle      dynamic      not acting
  english (lack)        empty / blank / void     structural   nothing inside slot
  english (openness)    free / open / pending    structural   uncommitted
  math                  zero / null              both         numerical / absence
  math (fn)             identity function        dynamic      f(x) = x, value passes
  physics (particles)   neutrino                 structural   one particle, barely interacts
  physics (bosons)      Z boson                  structural   neutral current particle
  isotope               ²H (deuterium)           structural   balanced p + n
  boolean               NOT (fp identity at 0)   dynamic      0 stays 0 under NOT
  combinator            K                        structural   ignores second arg
  programming           no-op / pass             dynamic      do nothing
  programming           null / void              structural   no value / no type
  color                 gray / middle gray 18%   structural   neither white nor black
  music                 rest (notation)          dynamic      one silence in sequence
  music                 fermata                  dynamic      one pause marker
  electricity           ground / neutral         structural   one reference potential
  electricity           no current               dynamic      one circuit net flow = 0
  magnetism             null point               structural   one spot between poles
  grammar               neuter gender            structural   one attribute uncommitted
  grammar               zero morpheme            structural   one slot unfilled
  law                   neutrality / abstain     dynamic      one voter uncommitted
  law                   pending / tabled         structural   one decision on hold
  sports                timeout / stalemate      dynamic      one game at pause
  sports                tie / draw               dynamic      one score balanced
  finance               break-even               dynamic      one ledger at zero
  finance               hold / no position       structural   one capital uncommitted
  seasons               equinox                  dynamic      one balance point day/night
```

## Gaps

- **TEEM / matrix cells**: the 5 frames × 3 PIE tenses matrix moved to
  ↕↕↕↕.md since most cells describe full-container rather than
  single-axis states. If single-↕ has its own matrix (e.g. ↕ at
  different dimensional scales), needs separate derivation.
- **↕ as IS tense**: if ↕ file IS the IS-tense, then per-frame content
  should be "IS at each dimensional floor" — one column per frame,
  not three. Current file doesn't yet have a frame breakdown —
  unclear what single-axis ↕ looks like at each floor.
- **Ambiguous rows**: physics "equilibrium" and several others could
  apply at either scope. Kept here pending clarification.
- **NOT as fp-identity**: ↕ = NOT only on the zero input (NOT(0) = 0
  in fp). Standard NOT flips; on zero input NOT is identity. So the
  ↕-mapping is specifically NOT-on-zero = pass-through.
