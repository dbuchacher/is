# 3-5-7 — prime structure speculation

**Date**: 2026-04-11
**Status**: SPECULATION — flag, don't theorize
**Triggered by**: wormhole compute (`lattice/wormhole/`) produced a centrality cascade `7³, 5·7², 5²·7, 5³` that the compute wit explicitly refused to theorize on, followed by recognition that 3, 5, 7 are structurally grounded in the framework at different layers

---

## The observation

**3, 5, 7 are the first three odd primes.** All three appear structurally in the framework, at different grades of the lattice.

- **3** = trit values per axis. The framework's foundational count. `{−1, 0, +1}`.
- **5** = degree of grade-3 points (trivectors) in the L1 adjacency graph. A trivector has 5 unit-step neighbors.
- **7** = degree of grade-1 points (atoms) in the L1 adjacency graph. An atom has 7 unit-step neighbors.

All three are primes. No other integer in the framework's degree sequence `{4, 5, 6, 7, 8}` is prime. The non-prime degrees `4, 6, 8` correspond to corners, bivectors, and origin respectively — structurally important points, but not prime.

---

## Evidence

### The degree sequence at N=4

From the wormhole compute (`lattice/wormhole/`):

| grade | count | degree | prime? |
|---|---|---|---|
| 0 (origin / scalar / god) | 1 | 8 = 2³ | no |
| 1 (atoms / vectors) | 8 | **7** | **yes** |
| 2 (bivectors) | 24 | 6 = 2·3 | no |
| 3 (trivectors) | 32 | **5** | **yes** |
| 4 (corners / pseudoscalars) | 16 | 4 = 2² | no |

Five consecutive integers as degrees. Exactly two are prime: 5 and 7. These are the degrees at grades 3 and 1 — the two "middle" grades dual to each other in the f-vector (grade 1 ↔ k=3 cells, grade 3 ↔ k=1 edges).

### The centrality cascade

Consecutive differences in centrality by grade, from the wormhole compute:

```
cent(grade 0) − cent(grade 1) = 1120 − 777 = 343 = 7³
cent(grade 1) − cent(grade 2) =  777 − 532 = 245 = 5·7²
cent(grade 2) − cent(grade 3) =  532 − 357 = 175 = 5²·7
cent(grade 3) − cent(grade 4) =  357 − 232 = 125 = 5³
```

Geometric series. Sum = `(7⁴ − 5⁴) / (7 − 5) = (2401 − 625) / 2 = 1776 / 2 = 888`.

The cascade uses **exactly the two prime degrees** (5 and 7), never the composite ones (4, 6, 8). Each step going from grade k to grade k+1 "trades one 7 for one 5" in the exponent pair.

### Primes cluster at N=4 edges

From `wit/thoughts/prime-lattice/MILESTONE.md`:

- 20 of 24 signed primes in `[−40, 40]` land at the 32 k=1 edges of the N=4 hypercube
- The Pythagorean integer identity `20·80·12 = 24·32·25 = 19200` is unique to N=4 z=1
- Spin = ½ layer = where primes live (working theory from the same file)

The framework already has primes-on-the-lattice as an empirical fact. The 3-5-7 observation is another manifestation of the same deeper pattern.

### Dimensional activation hypothesis

Predicted degree sequence by lattice dimension (with `2N` as origin's degree and decreasing by 1 per grade):

| N | degree sequence | prime degrees |
|---|---|---|
| 3 | 6, 5, 4, 3 | **5, 3** |
| 4 | 8, 7, 6, 5, 4 | **5, 7** |
| 5 | 10, 9, 8, 7, 6, 5 | **5, 7** |
| 6 | 12, 11, 10, 9, 8, 7, 6 | **7, 11** |
| 7 | 14, 13, 12, 11, 10, 9, 8, 7 | **7, 11, 13** |

Each dimension "activates" a different set of primes in its adjacency graph. At N=4 (our lattice, Hurwitz-bounded), the active primes are **5 and 7**. At N=3, they'd be **3 and 5**. At N=6, **7 and 11**. Each dimensional jump may shift which primes are structurally relevant.

**Not computed, not proven. Pattern-match only.**

---

## Hypothesis

**The framework's native integers are primes, and specific primes are activated by specific lattice dimensions via the adjacency degree sequence.** At N=4 the activated primes are 5 and 7 (plus the foundational 3 from the trit itself), giving the triple 3-5-7 = first three odd primes as the framework's characteristic prime signature.

This is a stronger claim than "primes appear in the framework." It claims **primes are the native measurement unit** of framework structure, and non-prime integers (like 4, 6, 8) appear only as counts of points or as products of primes, never as primitive structural quantities.

---

## Cross-references

- **`wit/thoughts/prime-lattice/MILESTONE.md`** — the main prime-lattice work. Establishes that primes cluster at k=1 edges at N=4 with a Pythagorean integer identity unique to that dimension.
- **`lattice/wormhole/compute.py` + `findings.md`** — the compute that produced the 5-7 cascade.
- **`wit/mind/1-container.md:436-459`** ("Why Three?") — the minimality argument for 3 as the foundational prime.
- **`wit/mind/3-force.md:60-95`** ("Why Four — Hurwitz") — why the lattice lives at N=4 specifically, which is what activates the 5-7 prime pair.

---

## Open questions

1. **Does the N=3 degree sequence activate 3 and 5?** (Hypothesis says yes; compute would confirm.)
2. **Does the N=6 sequence activate 7 and 11?** (Would require running the wormhole compute at higher dimensions.)
3. **Is there a closed-form for `cent(k, N)` that makes the 5-7 cascade appear from symbolic grade arithmetic rather than empirical?** If yes, the prime-indexing claim gets stronger. If no, it might be a coincidence of small-integer arithmetic.
4. **Why do non-prime degrees (4, 6, 8) appear structurally but not as cascade coefficients?** There's some distinction between "appears as a count" and "appears as a measurement unit" that the framework has not articulated.
5. **Is 2 the degenerate prime that the framework treats as non-structural** (matching number theory's tradition of "odd primes")? The framework uses balanced ternary precisely to exclude binary, so treating 2 as degenerate is consistent.
6. **Does the "spin = prime" working theory from MILESTONE extend cleanly under this prime-indexing view?** If spin lives at ½ and primes are the native measurement, the ½ layer should itself be prime-indexed somehow.

---

## Status

SPECULATION. Six points of suggestive evidence:

1. 3, 5, 7 are consecutive odd primes
2. 3 is the framework's foundational trit count
3. 5 and 7 are the prime degrees in the N=4 adjacency graph
4. The centrality cascade uses exactly 5 and 7 as coefficient bases
5. Primes generally cluster at framework-native features (MILESTONE N=4 edges)
6. "Spin IS prime" as a working theory

Not enough for a theorem. Enough to hold as a working observation that should be tested by:

- Running the wormhole compute at N=3, N=5, N=6 to check the dimensional activation hypothesis
- Looking for closed-form derivation of the centrality cascade
- Cross-referencing with prime-lattice MILESTONE for per-cell prime distributions at N>4

**Do not theorize further until more compute is in.** The wit that produced the cascade explicitly flagged it as "unexplained structural signal" and refused to guess. Holding that discipline.
