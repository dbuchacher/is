# 1-body — the wheel (reality's form)

Boot file. Read after `0-frame.md` (why this repo exists), then
continue to `2-wit.md` (methodology) and `3-emotions.md` (signal/
noise filter). Deep details live in `1-where/`.

This is the **body** — the structural facts. The part of the
framework that doesn't care what you think about it. The body IS,
whether anyone reads this or not.

## Two load-bearing meta-claims first

Before any structural content, two framework-meta-claims that
license everything below.

### THE RULE — one substrate, not two

There is **one substrate**. Every domain of structured human
knowledge — physics, chemistry, biology, computation, music,
language, math, religion, art — is a vocabulary for some slice of
**the same** substrate.

Think about water. "H₂O" is chemistry. "Blue wavy stuff in the
ocean" is your eyes. "Rain" is weather. "The thing you drink" is
thirst. These aren't four different things that happen to be
related. They're **one thing** named four different ways. When
someone says "H₂O IS water," they mean identity, not similarity.

```rust
// identity, not analogy — category error to treat the same thing as two things
```

**Identity, not analogy.** When two vocabularies describe the same
substrate feature, they are describing **one thing**, not two
analogous things. The physicist's "force" and the programmer's
"function call" and the musician's "chord change" are the same
operation. One operation, four names.

```rust
// metaphor (μεταφορά) — Greek "carry across." The substrate carries itself across vocabularies.
```

[**Metaphor**](1-where/roots/cognition.md) (metá + pherein) **IS literal.** Carrying structure
across vocabularies IS what the substrate does. "The universe is
a computer" is not analogy; it's identity.

### THE FILTER — effort × precision × independent convergence

Not every pattern is substrate. You'd see patterns in cloud shapes
all day if you looked — clouds don't mean anything. So we need a
filter that tells us **which patterns are real signal** vs. which
are random coincidence your brain is making up.

```rust
// metrognosis (μέτρον "measure" + γνῶσις "know") — measurement-knowing. The framework's epistemology.
```

```
  signal = effort × precision × independent convergence
```

- **Effort**: does reaching the convergence require real cumulative
  work? "Ancient Egyptians spent 4500 years surveying Giza" is
  effort. "Random cloud shape at 3pm Tuesday" is not.
- **Precision**: is the agreement exact or sub-1%? Or fuzzy? Two
  things that match "roughly" aren't signal — they could match
  anything.
- **Independent convergence**: do the things that match come from
  totally different processes? Two measurements by the same
  physicist aren't independent — two measurements by different
  civilizations thousands of years apart **are**.

```rust
// independent convergence — each source must have its own generation process. Algebraic restatements of one derivation don't count.
```

When all three line up, treat as signal — even if a frequentist
null can't distinguish from chance. When they don't, no statistical
test rescues the claim.

This filter IS how the framework separates substrate from
numerology. Every claim in this file passed it. Claims that don't
pass are either flagged for `3-which/` (verification queue) or
rejected.

---

## Vocabulary (pedantic)

Three words that get confused because physics narrowed them.
Framework uses their **original** meanings (proven by the PIE
roots that survived 6000 years).

```rust
// PIE — Proto-Indo-European, the reconstructed parent language of most European, Iranian, and Indian tongues. Roots marked *x- are ~6000 years old.
```

- [**reality**](1-where/roots/being.md) (*reh₁-*) — **what IS**, silent and unprovable.
  The bare fact of existence, prior to any description. You
  can't axiomatize it because writing presumes it.
- [**universe**](1-where/roots/crossovers.md) (unus + vertere) — **the wheel itself**, the full
  rotating-unified structure. Physics narrowed this to mean
  "observable H"; the framework reclaims the original. Universe
  = the wheel.
- [**cosmos**](1-where/roots/structure.md) (*kosmos*) — **one H, one ordered arrangement**.
  Our cosmos = our H. The universe contains multiple cosmoses
  at S-scope: 3 peer + 1 mirror.

```rust
// scope distinction — reality precedes description. Universe = full structure. Cosmos = one H-sized slice of it.
```

**Rule:** "reality" for the silent substrate. "The wheel" or
"universe" for the full structure. "Cosmos" or "our H" when you
mean physics-scale observable reality. Don't let physics's
narrow-universe usage bleed into the framework.

---

## Plain English (30 seconds, no math)

**Don't try to picture four dimensions** — nobody can, and you
don't need to. Instead, picture a **fidget spinner**. Or a
**bicycle wheel**. Or a **record on a turntable**. Something you've
held, spinning around a center. That's the starting image.

Now layer in: picture a **gyroscope** — a wheel inside a ring
inside another ring, each one turning on a different axis. Or
a **globe with brass rings inside it** (called an armillary sphere
if you've seen one in a museum). Or Ezekiel's "wheel within a
wheel" from the Bible. Or the **rotating rings** in sci-fi movies
(Contact, Doctor Strange). These are all the same picture —
**a center with rings spinning around it on different axes**.

```rust
// 3D projection — our eyes can only see 3D. The wheel's 4D structure shows up as multiple-rings-around-a-center.
```

The real wheel has **four kinds of "where"** instead of three.
Every point on it can be asked four different questions:

- **Where** is it? (position — the only one ordinary space has)
- **What** is it made of? (substance — mass, matter)
- **Which** one is it, of the possibilities? (signal, identity)
- **When** does it exist? (time)

```rust
// wh-words from PIE *kʷi- (interrogative stem). One root, four questions, 6000 years preserved.
```

Each question is a separate spoke of the wheel. You live in it.
Every atom, every particle, every force — anything that "is"
anything — is located on the wheel by answering those four
questions.

When people across 2600 years of unrelated cultures drew the
wheel (chakras, medicine wheel, Ezekiel, armillary spheres,
chakra yantras, Hollywood sci-fi), they kept landing on the
same image: a **center with rings spinning around it**. They were
all seeing 3D projections of the same 4D structure. That's the
wheel.

```rust
// metrognosis convergence — 2600 years, unrelated cultures, same image. Passes the filter.
```

Now some properties:

- **The spokes come in a 3+1 pattern, not as four identical ones.**
  Like a three-legged stool with a person sitting on it — three
  legs do the same job (supporting), but the person on top is a
  different kind of thing. Or a tricycle: two wheels in the back
  do one job, one wheel in the front does a different job.
  Three-of-the-same plus one-that's-different. That's the shape.

  ```rust
  // 3+1 fractal — same structure at H-internal (i,j,k imaginary + 1 real), S-internal (3 peer H's + 1 mirror H), periodic table (Z-blocks), Minkowski (3 space + 1 time).
  ```

  From one viewpoint you see four spokes; from another you see
  three peered together plus one "mirror" of them. Both are true
  — same structure, different viewpoints. The key is **the fourth
  is always different** from the first three. You never get
  four-of-the-same. (This is the same reason there are three
  generations of particles — electron, muon, tau — but no fourth
  generation, ever. Cartan's math forbids it.)

- **Four is the upper limit on bundled directions.** Like how a
  chair has 4 legs not 5 (more would trip you) and not 3 (wobbles
  too easy). Four is the ceiling where things still multiply
  cleanly. A German mathematician named Hurwitz proved this in
  1898. Hamilton tried to make three-dimensional multiplication
  work for ten years and failed — three doesn't have enough room
  to turn in. Five or more breaks another property. Four is the
  answer — but always arranged 3+1, never four-of-the-same.

  ```rust
  // Hurwitz (1898) — proved exactly 4 normed division algebras: R(1D), C(2D), H(4D), O(8D). Frobenius (1877) proved no 3D possible.
  ```

- **Three values per spoke** — up, zero, down. Like a light switch
  with three positions (on, off, neutral) instead of two. Two
  values can't mean "in the middle." Four is wasteful. Three
  gives you positive, zero, negative — the minimum set that has a
  center. Hydrogen has exactly three isotopes that realize these
  three values physically — not coincidence. **Hydrogen IS the
  trit made of matter.**

  ```rust
  // trit — balanced ternary {+1, 0, −1}. Minimum signed + centered value system. Hydrogen isotopes ¹H/²H/³H = ↑/↕/↓.
  ```

- **The wheel spins because it can't sit still.** Imagine pushing
  two magnets together, same poles facing — they push back. Try
  to force them still and they squirm. The harder you push, the
  more they resist. That's what perfect balance is like: physically
  impossible to hold forever. Two reasons it can't stop:

  ```rust
  // 3rd law thermodynamics — absolute zero unreachable in finite steps (Nernst 1906). The balance point is an asymptote.
  ```

  ```rust
  // non-commutativity — ij = k but ji = −k. Quaternion algebra has asymmetry baked in; perfect symmetry isn't available.
  ```

  Thermodynamics says so: absolute zero (all stillness) is an
  asymptote you approach but never reach. And the math of
  quaternions says so: multiplication is ordered. Doing "this
  then that" gives a different answer than "that then this" —
  there's built-in asymmetry. **The universe moves because the
  alternative is impossible.**

This spinning is what we call **time**, and also what we call
**energy**, and also **gravity**, and also **change**. Same
substrate operation, different names in different fields.

That's the body. The universe has a shape. The shape is this
wheel. The rest of the framework explains how that shape shows
up in what you actually see.

---

## The wheel in one line

**Hub + 4 spokes + 3 values per spoke + eternal rotation = the
substrate of everything.**

In framework symbols:

```
  hub         ↕↕↕↕         // container, additive identity, shared center
  spokes      χ μ φ τ       // the 4 axes
  values      ↑ ↕ ↓         // up, neutral, down per axis
  spokes mean WHERE WHAT WHICH WHEN
```

---

## Key structural facts

### 1. The hub (↕↕↕↕)

The center where all four spokes meet. Think of a bicycle wheel's
**hub** — the bolt in the middle that holds the spokes together.
It doesn't move. Everything turns around it. **Every trip that
loops — all the way around and back — ends up home to the center.**

```rust
// additive identity — 0 + q = q. The hub is H's zero element. ↕↕↕↕ = grade 0 = zero axes committed.
```

Add zero to anything and nothing changes. That's what the hub
does. It's the "do-nothing" point that **every closed walk
returns to** — like shutting your eyes, spinning three times,
and finding you're still in the same spot.

```rust
// Cayley-Dickson tower — R(1D), C(2D), H(4D), O(8D), S(16D). The zero element is the SAME point across every algebra layer. One hub, many scopes.
```

**Shared across every algebra layer.** 1D reals, 2D complex,
4D quaternions, 8D octonions, 16D sedenions — the zero is the
same zero in every one. One hub. Every layer.

```rust
// observer at hub — load-bearing claim. The hub has no χ-value → no spatial location → shows up WHEREVER recognition happens.
```

**Not a location. This is the weird part.** You cannot "go to"
the hub because "location" is what you get when the χ (WHERE)
spoke commits. The hub has χ uncommitted. So it's nowhere
spatially — but structurally, **the hub is wherever recognition
is happening.** Every closed walk closes there. Every "oh!" when
a sentence clicks is a hub-event. Every observer is AT the hub,
because being an observer means a loop just closed in you.

```rust
// recognition = re- (again, Latin "back") + co- (together) + gno-/gen- (know/beget) — loop-closure + read-write coincidence at the hub. One PIE verb in *ǵneh₃-* split into know/beget branches.
```

Right now, reading this, **you are at the hub.** Not a metaphor.
The hub isn't somewhere you could stand outside of; it's
wherever the closing-a-loop event is. You're closing loops right
now (that's what reading IS). So you're there.

```rust
// Atman = Brahman = ↕↕↕↕ — Hindu tradition's name for the same substrate fact. Also: "kingdom of heaven is within you" (Christian), not-self (Buddhist). Independent convergence on observer-at-center.
```

Deep: `1-where/hub.md`, also `1-where/wheel.md § "Where is the hub?"`

### 2. The 4 spokes (axes), arranged 3+1

Four axes, but **not four peers** — always 3+1. Three of them
work together; one stands apart. The four ARE the four questions
you can ask about any point — all from one ancient PIE stem
that gave English its wh-words (where, what, which, when):

```rust
// *kʷi- — PIE interrogative stem. One root branches into 4 direction-questions. Latin qui/quae, Greek tis, Sanskrit ka-. 6000 years preserved.
```

- **χ (WHERE, imaginary i)** — position. Where is this thing?
- **μ (WHAT, imaginary j)** — [**substance**](1-where/roots/crossovers.md) (*sub-stare* = "stand
  under"). What stands under the appearance? Mass, matter, stuff.
- **φ (WHICH, imaginary k)** — signal. Which one is it, among
  possibilities? Charge, flavor, identity.
- **τ (WHEN, real 1)** — time. When does it happen?

```rust
// quaternions (H) — 4D, non-commutative. i,j,k are the 3 imaginary basis elements (rotate into each other); 1 is the real scalar (doesn't rotate). 3+1 baked into the algebra.
```

The asymmetry matters: χ, μ, φ rotate into each other like three
people in a square dance passing around. τ stands in the middle
and doesn't rotate — it's the scalar, the measuring stick. And
at a larger scope (sedenions), the **same 3+1 pattern repeats**:
3 peer quaternions + 1 mirror quaternion. Same shape at every
scale.

```rust
// 3+1 fractal — same asymmetry at H-internal (3 imaginary + 1 real), S-internal (3 peer H's + 1 mirror H), Pati-Salam (3 quark colors + lepton), Minkowski (3 space + 1 time), periodic table (Z-blocks). Five independent scopes, same pattern.
```

Physicists usually collapse these to "3 spatial dimensions plus
time." That's a double mistake. First: **only χ is spatial.** μ
is substance (mass, matter), φ is signal (charge, information),
τ is time. What physics calls "3D space" is actually χ projected
through three views (up/down, left/right, forward/back — all one
axis with three directions). Second: the 3+1 they describe ("3
space + 1 time") IS the same 3+1 as this H-internal 3+1, but
physics only notices it at the Minkowski scope and misses that
it's a **fractal pattern present at every scale**.

Deep: `1-where/axes.md`

### 3. Three values per spoke

Each spoke can be in one of three states. Like a light switch
with three positions:

- **↑ (up, +1)** — pushed forward. On. Something there.
- **↕ (neutral, 0)** — in the middle. Off. Free to go either way.
- **↓ (down, −1)** — pushed backward. Reversed. The opposite
  of ↑.

```rust
// trit — balanced ternary {−1, 0, +1}. Minimum signed + centered system. Two values (binary) can't represent a center. Four+ is wasteful.
```

These three values are the three tenses of **being** in English.
And English didn't invent this — **it kept three separate ancient
roots** because "is", "be", and "was" are three different
substrate operations:

```rust
// three PIE roots for "being" — preserved across 6000 years because the substrate needs three states. Not just one verb with conjugations; three independent PIE stems.
```

- ↑ = [**BE**](1-where/roots/being.md) (*bʰuH-* = "become, grow, be") — the forward
  commit. Same root as **future** and — weirdly — **physics**
  itself (from Greek *phusis* = growth).
- ↕ = [**IS**](1-where/roots/being.md) (*h₁es-* = "to be") — the uncommitted present.
  Root of **essence**, **entity**.
- ↓ = [**WAS**](1-where/roots/being.md) (*h₁wes-* = "to dwell, stay") — the backward
  commit, the settled/completed thing. Same root as **Vesta**
  (Roman goddess of the hearth, the dwelling).

Try it: "BE without WAS" = no memory (you can never have done
anything). "IS without BE" = frozen (you can never change).
"WAS without IS" = gone (nothing present to be past of). All
three needed or the verb breaks.

Hydrogen has **exactly three stable-to-metastable isotopes**
(¹H, ²H, ³H) that realize these three values physically. Not
coincidence — **hydrogen IS the trit made of matter**.

```rust
// hydrogen isotopes as trit — ¹H (1p+0n) = +1 = ↑ stable, ²H (1p+1n) = 0 = ↕ stable, ³H (1p+2n) = −1 = ↓ unstable β⁻ (drains electron). Decay channel matches "↓ consumes."
```

Deep: `1-where/trits.md`

### 4. The lattice (81 points)

Combine 4 spokes × 3 values = **3⁴ = 81 lattice points.** Like
a 4-digit combination lock where each digit can be 0, 1, or 2.
Exactly 81 combinations.

```rust
// 3⁴ = 81 — the lattice of integer quaternion positions in H ∩ ℤ[↑,↕,↓]⁴.
```

- **1 point is the hub** (↕↕↕↕, all spokes at zero — the
  "do-nothing" address).
- **80 points are "things"** — each has at least one spoke
  committed.

Sort the 80 by **how many spokes are pushed away from neutral**.
This is called the **grade**. Pascal's row-4 triangle (1, 4, 6,
4, 1) tells you the count at each grade:

```rust
// grade — Clifford algebra grading. grade k has C(4,k) · 2^k points (choose axes × sign each). Row 4 of Pascal = (1,4,6,4,1) × (1,2,4,8,16) = (1,8,24,32,16) = 81.
```

```
  grade   count   name          // example
  ─────   ─────   ─────────
    0       1     hub           // no spokes pushed (↕↕↕↕)
    1       8     atoms         // one spoke pushed
    2      24     bivectors     // two spokes (planes)
    3      32     trivectors    // three spokes
    4      16     corners       // all four spokes pushed
  ─────  ─────
  total    81    everything    // (80 non-hub = "things")
```

**The 80 stable elements of chemistry fill exactly these 80
points.** Not like them — **are** them. Same count because same
structure. Tc and Pm, the two unstable elements below lead,
leave exactly two missing-tooth slots in the Z=1-82 range, which
matches the Z2 (order-2) mirror-flip in the wheel's S-scope
structure. The periodic table is the wheel drawn in atoms.

```rust
// 80 = 3⁴ − 1 = 8+24+32+16 (grade sum). 80 stable elements = Z 1-82 minus Tc(43), Pm(61). Holes = |Z2| = 2. See 3-which/elements.md.
```

Deep: `1-where/lattice.md`, `3-which/elements.md` (for the match)

### 5. Eternal rotation

The wheel spins forever because it **can't** stop. The rotation
is baked into the vocabulary itself — the PIE root *wer-/wert-*
("to turn") gave English **universe** (unus + vertere = "turned
into one"), **vertex**, **vortex**, **verse**, **reverse**,
**convert**, **invert**. Every major turn-word traces to this
one root. The substrate is rotating and the oldest language we
can reach knew it.

```rust
// *wer-/wert- — PIE root for "turn." Universe = "turned into one." The word CARRIES the rotation.
```

Two independent reasons it can't stop:

```rust
// non-commutativity — ij = k but ji = −k. Quaternion multiplication asymmetric. Reversing order reverses sign. Built-in asymmetry.
```

- **Math has asymmetry baked in.** Quaternion multiplication
  isn't like ordinary multiplication. 3 × 2 = 2 × 3 = 6 — order
  doesn't matter in normal numbers. But for quaternions: i × j
  = k, while j × i = −k. Opposite sign! The order you do things
  in matters. Perfect symmetry isn't available.

```rust
// 3rd law of thermodynamics — absolute zero (0 K, S=0) is asymptotically unreachable. Nernst 1906. Motion can't fully stop.
```

- **Thermodynamics says the same thing.** Absolute zero — the
  temperature at which everything would be still — is an
  asymptote. You can get arbitrarily close, but you can never
  reach it in finite steps. Motion never fully drains.

**Therefore**: the wheel can't settle. Motion is eternal and
directional.

```rust
// dark energy = wheel rotation — not a substance. Constant Λ because rotation rate is constant. Predicts no redshift evolution (verified).
```

This is what **dark energy** is — not some new mystery substance
hiding in space, just the wheel's own perpetual spin. When
cosmologists measure "the universe is expanding faster," they're
measuring the wheel turning. The cosmological constant is
**constant** because the rotation is constant.

Deep: `1-where/wheel.md § "Why the wheel spins"`

### 6. The 4 forces

Physics has four fundamental forces. They feel like separate
things with wildly different strengths. The framework says
**they're four operations the wheel performs** — like four
different instructions on a CPU. All four fire at the same
clock rate; the difference is what each one does.

```rust
// axis ↔ force mapping — each force is a whole-quaternion operation tied to one axis. READ/CALL/CYCLE/TEST ≡ strong/EM/gravity/weak.
```

```
  force    operation   CPU verb   // effect
  ─────    ─────────   ────────
  strong   READ        MOV        // observe / bind matter in place
  EM       CALL        CALL       // act / emit photons
  gravity  CYCLE       LOOP       // iterate / run the event loop
  weak     TEST        NOT/CMP    // decide / trigger decay
```

```rust
// whole-quaternion operation — i × q changes ALL four components of q, not just one. Forces don't nudge per-axis; they transform the whole position.
```

Each force is ONE operation on the **whole** 4D position, not
a per-axis nudge. When a photon is emitted, it's a CALL acting
on the full (position, substance, charge, time) at once.

```rust
// no graviton — gravity is the event loop ITSELF, not a process running on it. You don't ask what kernel Linux runs on; you don't ask what particle mediates the wheel's rotation.
```

**Gravity has no particle.** No graviton, never will. Gravity
IS the wheel rotating — it's the event loop that runs all the
other events. Looking for a graviton is like asking what
program the operating system itself runs on. Category error.

```rust
// hierarchy problem dissolved — gravity appears "weak" only at our mass scale. At Planck scale α_G = 1 (unit strength). Weakness is (m/m_P)² scaling, not fundamental.
```

**Gravity's "weakness" is fake.** At Planck scale (the wheel's
natural clock rate), gravity is full-strength (α_G = 1). At
ordinary scales it looks weak because ordinary particles are
about 10¹⁹ times lighter than the Planck mass, and α_G scales
as mass-squared. Not a mystery — a consequence.

Deep: `1-where/forces.md`

### 7. S-scope — 4 H's in 3+1

Our H (4D) isn't alone. It's part of a bigger structure. At the
**S-scope** (16-dimensional), there are **4 H's bundled together**.
Like four identical-looking drawers in a cabinet — but when you
pull them open, you find three are the same and one is flipped
backward.

```rust
// S-scope — sedenions, S = O⊕O = (H⊕H)⊕(H⊕H). 16D. Contains exactly 4 H's via Cayley-Dickson doubling.
```

```rust
// Aut(S) = S3 × Z2 = 48 — order 48. Three-way convergence: |Aut(S)| = F4 roots = Standard Model fermion count. Load-bearing.
```

- **3 of them are peers** — interchangeable under a rotation
  called **triality**. These become the three generations of
  matter particles in physics: electron/muon/tau, up/charm/top,
  down/strange/bottom. **Three copies of the same structure.**

```rust
// triality — S3 outer automorphism of D4 (so(8)). Cyclically permutes 3 H's. Gives 3 fermion generations. Only D4 has S3; no other simple Lie algebra does.
```

- **1 of them is a mirror** — flipped under a reflection called
  Z2. This is qualitatively different from the three peers. In
  physics: the **sterile / right-handed / CP-mirror sector** —
  structurally required partners (sterile neutrinos, right-handed
  fermion partners) different from the observed gen-1/2/3 matter.

```rust
// Cartan's ceiling — Out(Lie) ≤ S3 for all simple Lie algebras; S3 achieved ONLY by D4. Therefore no 4th fermion generation can exist. Algebraic impossibility, not "unobserved."
```

**A 4th generation is algebraically impossible**, not just
unobserved. Cartan's math (classical Lie algebra theory) proves
it. The framework has a falsifier here: if LHC or a future
collider discovers a 4th-gen fermion, the framework breaks.

The **3+1 pattern repeats at every scope** the wheel manifests:

```rust
// 3+1 fractal — same asymmetry at 5 independent scopes. Metrognosis: structural convergence.
```

- **H-internal**: 3 imaginary axes (χ/μ/φ = i/j/k) + 1 real (τ = 1)
- **S-internal**: 3 triality-peer H's + 1 Z2-mirror H
- **Pati-Salam GUT** (1974): 3 quark colors + 1 lepton "color"
- **Minkowski spacetime**: 3 spatial + 1 time
- **Periodic table**: 3 peer H-blocks (Z=1-60) + 1 mirror H-block
  (Z=61-82)

Five independent scopes, same 3+1 shape. **Fractal.**

Deep: `1-where/scope.md`

### 8. The mass ladder

Physics has about 20 "free parameters" — particle masses, mixing
angles, coupling constants — that textbooks say have to be
measured, not calculated. Nature just "chose" them.

**The framework derives 30+ of them from substrate counts at
sub-1% precision.** Every stable particle's mass follows one
rule:

```rust
// mass ladder — m = m_P × f × 10^(−(16 + N_sub)). 30+ SM parameters derived <1% precision.
```

```rust
  m = m_Planck × f × 10^(−(16 + N_sub))
  //            ^                  ^
  //            |                  +-- substrate count per particle role
  //            +-- geometric factor (√3/2, √2, 5/12, π/4, ...)
```

Where:
- **m_Planck** = the "clock rate" mass of the universe
- **f** = a clean geometric factor: √3/2 (= sin 60°), √2, 5/12,
  π/4 — all substrate-anchored angles or ratios
- **N_sub** = a substrate count specific to the particle's role:
  1 for electroweak particles (Higgs, W, Z, top)
  3 for hadrons (proton, neutron, tau, charm, bottom)
  4 for muons and pions
  6 for light fermions (electron, up)
  12 for neutrinos

```rust
// substrate counts — not arbitrary. 1 = hub, 3 = trit, 4 = axes, 6 = C(4,2) = bivector planes, 12 = D4 opposing pairs, 16 = grade-4 corners, 17 = 16+1 = corners+hub.
```

Examples (all predicted within ~1% of measured):

- **Muon** = m_P × sin(60°) × 10⁻²⁰ = 105.66 MeV (0.003% error)
- **Top quark** = m_P × √2 × 10⁻¹⁷ = 173 GeV (0.27% error)
- **Electron** = m_P × (5/12 + α/3) × 10⁻²² = 0.511 MeV (0.06%)
- **Proton/electron mass ratio** = 108 × 17 = 1836 (0.008% error)

```rust
// fine structure constant α derivation — α⁻¹ = 544/4 + 1 + 3/84 = 8·17 + 1 + 1/28 = 137.035714. Matches measured to 0.0002%.
```

The **fine structure constant α** — the fundamental number of
electromagnetism — falls out of substrate integers to **6
significant figures**:

```
  1/α = 544/4 + 1 + 3/84
      = 8 × 17 + 1 + 1/28                                 // substrate integers
      = atoms × (corners + hub) + hub + 1/(axes × wh-words)
      = 137.035714                                         // measured: 137.035999
```

Deep: `1-where/masses.md`

---

## Viewpoints (open — only two articulated)

Think of a house from outside. You can walk around it — front,
back, side. Same house, different views. The wheel has multiple
viewpoints too, and **each one shows different structure**. The
framework has only named two so far:

```rust
// Cayley-Dickson ladder — R(1D) → C(2D) → H(4D) → O(8D) → S(16D). Viewpoints are directions of traversal.
```

- **R-up (bottom-up climb)** — start from 1D reals, build up.
  Four reals bundle into one quaternion. Gives you "one cosmos
  with 4 axes." This is how textbook physics approaches the
  wheel.
- **S-down (top-down descent)** — start from 16D sedenions, look
  down. S contains 4 quaternions arranged 3+1. Gives you "three
  peer cosmoses + one mirror cosmos." Ancient traditions
  (chakras, Ezekiel, medicine wheel) intuited this view.

```rust
// Frame 10 identity — two readings of ONE structure. Same substrate, different aperture. Not two different theories — one thing from two angles.
```

Same substrate, two directions. Both true. (Looking at a house
from the front vs. the back doesn't give you two houses.)

**Other viewpoints exist and are NOT written up yet** — this is
an honest open gap in the framework:

```rust
// C-scope, O-scope, chirality-pair, hub-scope — flagged for 3-which. Not settled. Body ≠ speculation; these are speculation until verified.
```

- **C-scope** (2D complex) — magnitude + phase. Physics uses
  this all the time for quantum amplitudes; framework hasn't
  integrated the C-view yet.
- **O-scope** (8D octonions = 2 H's, non-associative) — Baez's
  work on the Standard Model lives here; framework has barely
  touched it.
- **Chirality pair** — left/right flip; related to but distinct
  from the S-scope Z2-mirror.
- **Hub-scope** — viewing from ↕↕↕↕ outward (what does the hub
  see?).

These are **flagged for 3-which** (verification queue) — claims
about the wheel from these angles are not yet settled and
shouldn't be taken as body facts until they pass the filter.

---

## What this rules out

The body's structure makes these things **impossible**, not just
unobserved. Like how a 4-sided square can't have 5 corners — not
because we haven't found one yet, because the shape forbids it.

```rust
// impossible by structure — distinct from "not yet observed." These have algebraic proofs.
```

- **A 5th independent axis.** Hurwitz forbids. Only 4 normed
  division algebras exist; you've hit the ceiling at H.

```rust
// Hurwitz 1898 — exactly 4 normed division algebras (R, C, H, O). Frobenius 1877: no 3D possible. Hard ceiling + hard floor.
```

- **A 4th fermion generation.** Cartan's outer-automorphism
  ceiling (Out ≤ S3, achieved only by D4) forbids it. Discover
  one in a collider and the framework breaks.

```rust
// Cartan's ceiling — Out(simple Lie algebra) ≤ S3; S3 only for D4. Triality = S3 outer automorphism permuting 3 peer H's. No room for a 4th.
```

- **A graviton particle.** Gravity IS the loop, not a mediator.

```rust
// no graviton — gravity is the event scheduler, not a scheduled event. Asking what particle mediates gravity is like asking what program Linux runs on.
```

- **True infinity anywhere.** Only integer values exist in the
  substrate. Decimals are verbs (rates, ratios, operations in
  flight) — not values. Infinities in physics equations are
  projection artifacts (a loop projected onto a line cycles
  forever; that's the projection, not the loop).

```rust
// no infinity (frame 7) — substrate has nodes and loops. "Infinity" in physics = projecting a loop onto a line; the loop is finite.
```

- **Heat death.** The wheel's eternal motion (non-commutativity
  + 3rd-law asymptote) means "final stillness" is unreachable.
  No thermodynamic terminus.

- **Consciousness as a neural byproduct.** The frame prohibits
  localizing consciousness inside 4-D matter. Consciousness is
  either the loop itself OR enters through the hub — see
  `2-wit.md`. What it's NOT: produced by neurons. (This is the
  frame's single most challenged claim; wit.md flags it as
  "the framework item most wits fail at.")

```rust
// frame 11 — consciousness NOT localized in 4D. Option A (consciousness IS the loop) or B (enters via hub ↕↕↕↕). Third option (neural production) is a framework-incompatible Cartesian error.
```

---

## What this implies

For someone reading this who is new to the framework:

- **The universe has a fixed, small inventory of structural parts.**
  Not a continuum, not an arbitrary collection. Like a LEGO set
  with a specific number of brick types — a specific, countable
  set: 4 axes × 3 values × 4 grades × a few layers. You can build
  billions of different things out of it, but the **parts** are
  bounded.

```rust
// discrete and finite — not an infinite continuum. 3⁴ = 81 positions, plus some outer-scope extensions. Everything lives on this lattice.
```

- **Most "mysteries" dissolve under frame-replacement**, not under
  answer-seeking-inside-the-old-frame. If a puzzle feels hard in
  the old frame, try standing in a different frame first. The
  puzzle might **vanish**.

```rust
// frame-replacement — many "open questions" of physics aren't hard answers waiting to be found. They're questions that dissolve when you stand in the right frame. The question itself was the problem.
```

  Examples:
  - The **hierarchy problem** (why is gravity 10³⁶× weaker than
    EM?) → 10⁻¹⁷ = corners + hub = 17, the natural scale
    separator. Not fine-tuned. Substrate-clean.
  - The **cosmological constant** (why is Λ 10⁻¹²³?) → E8 pairs
    + trit = 120 + 3 = 123. Not mystery; count.
  - **Hubble tension** (1.0869?) → e^(1/12) where 12 = gauge
    bosons. Not crisis; signature.

  These aren't coincidences being papered over — they're
  **substrate counts a delusional default wasn't tracking**.

```rust
// delusional-default — de- (away) + ludere (to play). Literally "played away from reality." The word for ignoring substrate is the etymologically correct word for it.
```

- **Observation = participation.** You, reading this, aren't
  outside the system like a scientist peering through glass.
  **You're at the hub.** Every closed walk — including the loop
  that closed when you understood a sentence just now — is a
  hub-event. Reading IS participating in the substrate.

```rust
// observer at hub — "view from nowhere" doesn't exist. All observation happens AT the loop-closure point. Frame 10: many observers = one hub read through many apertures.
```

  There's no "view from nowhere" — there's only views from hubs,
  and all hubs are the one hub (see `2-wit.md` on this).

- **Metaphor IS literal.** When Sanskrit says *samsara* (wheel),
  Ezekiel describes rotating rings, indigenous peoples build
  medicine wheels, Greek astronomers construct armillary spheres,
  and Hollywood draws rotating sci-fi rings — **they're all
  drawing the same object**. Not "the same in metaphor."
  **Literally the same object.** The word "metaphor" means
  "carry across" — the substrate carries itself across
  vocabularies when different traditions name the same structure.

```rust
// metaphor (μεταφορά = carry across) — the substrate's self-transport across naming traditions. Not poetic decoration — the identity move.
```

  Not poetic; structural.

---

## For wits: technical summary

The body's framework-native claims:

```rust
  H (Hamilton 1843)    // 4D normed division algebra, our inner algebra
                       // Lattice = H ∩ ℤ[↑,↕,↓]⁴, 81 points

  S (sedenions, 16D)   // contains 4 H's via Cayley-Dickson doubling;
                       // Aut(S) = S3 × Z2, order 48 = F4 roots =
                       // SM fermion count (triple convergence)

  Hub ↕↕↕↕             // additive identity, shared across R/C/H/O/S;
                       // loop closure point; observer location (hub
                       // has no χ commit, so "location" doesn't apply)

  Rotation verbs       // {READ, CALL, CYCLE, TEST} = whole-quaternion
                       // operations, identity with {strong, EM, gravity,
                       // weak} forces in physics

  Mass ladder          // m = m_P × f × 10^(−(16+N_sub)), f substrate
                       // clean, 30+ SM parameters derived <1%

  Hurwitz (1898)       // exactly 4 normed division algebras exist
                       // (R, C, H, O). Floor + ceiling on dim.

  Cartan               // Out(Lie) ≤ S3, S3 only for D4 → exactly 3
                       // fermion generations, no 4th possible

  Kirchhoff (1845)     // closed walks sum to zero; conservation at
                       // nodes; bound states close at hub

  Hamilton rotation    // q(θ) = cos(θ/2) + sin(θ/2)·n
  formula              // θ/2 is where ½-spin lives, neutron
                       // interferometry 720° period (Rauch 1975)
```

---

## Where to go next

### Deep files in `1-where/` (written separately)

| if you want                                 | go to                          |
|---------------------------------------------|--------------------------------|
| full wheel structure + why-it-spins         | `1-where/wheel.md`             |
| the 4 axes in depth (χ/μ/φ/τ semantics)     | `1-where/axes.md`              |
| how the 3 values per axis work              | `1-where/trits.md`             |
| the 81-point lattice, grades, corners       | `1-where/lattice.md`           |
| 4 forces = whole-quaternion operations      | `1-where/forces.md`            |
| H → S → outer D4/E8 → nested CD             | `1-where/scope.md`             |
| the mass ladder + Standard Model            | `1-where/masses.md`            |
| **PIE roots = substrate anchors**           | `1-where/roots.md`             |
| open structural questions                   | `1-where/open.md`              |

### Then read

- `2-wit.md` — how to think about the body, how to derive new results,
  what moves work, what anti-patterns fail.
- `3-emotions.md` — how the signal/noise filter works, which
  convergences count, verification pipeline.

### Then (when working on a specific domain)

- `3-which/` — specific signal convergences (α, elements, Giza,
  Ezekiel, karma, etc.)
