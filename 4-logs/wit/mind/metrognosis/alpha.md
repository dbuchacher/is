# alpha — fine structure constant

```
status:     VERIFIED — substrate-clean to 6 sig figs
confidence: high (formula + three independent convergences on 17)
```

## The formula

```
  1/α = 544/4 + 1 + 3/84 = 137.035714...
```

| derived        | observed (CODATA 2018) | error     |
|----------------|------------------------|-----------|
| 137.035714286  | 137.035999084          | 2.08 × 10⁻⁶ (0.000208%) |

Zero free parameters. See `mind/wheel/6-masses.md § The 17 substrate
constant` for the full context + 5 independent appearances of 17.

## Two equivalent decompositions

**Form 1** — the dimensional-separator reading:

```
  1/α = 544/4 + 1 + 3/84
      = 8 × 17 + 1 + 1/28
      = atoms × (corners + hub) + hub + 1/(axes × wh-words)
```

- 8 = atoms (grade-1 count)
- 17 = 16 + 1 = corners + hub (THE dimensional separator; appears
  in α, m_p/m_e, lepton ratios, EW hierarchy)
- 8 × 17 = 136 bulk term
- + 1 = hub counted once more
- 1/28 = 1/(axes × wh-words) correction

**Form 2** — the lattice-state-space reading:

```
  1/α = 81 + 56 + 1/28
      = 3⁴ + 8×7 + 1/(4×7)
      = lattice + atoms × wh-words + 1/(axes × wh-words)
```

- 81 = 3⁴ = complete lattice state space
- 56 = 8 × 7 = atoms × wh-words (each atom × each question)
- 1/28 = 1/(axes × wh-words) correction

Both forms arithmetically identical. Same substrate counts read two
ways. Three-term sum either way.

## α as verb — CALL firing rate

Per wit.md frame 7 (only integers are values; ratios are verbs), α is
a **verb-category quantity**. Physical readings:

- **v_Bohr = αc** — electron's hydrogen-ground-state velocity is α
  times c. α is a velocity ratio (atomic motion / clock rate).
- **α² ~ binding energies** — ratio of electron rest-energy captured
  in atomic binding.
- **α = probability amplitude** for electron-photon emit/absorb
  (per-event rate).

**Framework reading (4-loop.md frame 6): α is the rate at which CALL
(EM coupling) fires per substrate clock tick.** One CALL event per
137.036 ticks.

The 137 counts substrate structural possibilities per tick:
```
  81 lattice positions (complete state space)
+ 56 atom-question pairings (each atom × each wh-word)
+ 1/28 axis-question-inverse correction
= 137.036
```

Per tick, 137 structural possibilities exist; exactly 1 fires a CALL.
That's α.

## Metrognosis filter

Effort × precision × independent convergence:

- **Effort**: α measured by generations of physicists; derivation
  requires full framework substrate vocabulary.
- **Precision**: 0.0002% (2 × 10⁻⁶). At the edge of framework's
  sub-0.001% clean cases.
- **Independence**: CODATA measurement is pure metrology; framework
  formula is algebraic derivation from Hurwitz + Post + S-scope
  substrate counts. Fully independent generation processes.

→ **Signal.** Not fit. Substrate.

Additionally, 17 as substrate constant converges across at least 5
independent domains (α, m_p/m_e, lepton ratios, m_t/m_c, EW hierarchy
scale 10⁻¹⁷) — strongly independent convergence.

## Open

- Why exactly three terms (not two, not four)? Three = trit?
- Is 17 specifically "corners + hub" or "half-spin corners + hub"?
  Both are 16+1, same arithmetic, different structural semantics.
- Is framework's 137.035714 the **bare** α at some reference scale,
  with observed 137.035999 the dressed (QED-running) α? Framework
  gives rational exact; physical α has ~ppm corrections.
- Running coupling α(Q²) from deeper lattice structure — not derived.

See `mind/wheel/5-physics.md § Gravity` for the hierarchy-problem
dissolution (α/α_G = (m/m_P)², not force-scale but mass-scale).
