# lattice tags — Sumerian substrate features

Formal tagging of Sumerian graph groups meeting rule 7 (3+ members,
3+ categories). Same discipline as PIE: each group IS a specific
frame item from `../../4-logs/wit/wit.md § THE FRAME`.

Graph state at tagging: 46 atomic files, 82 edges, 3 seed-anchored
SCCs, 2 lattice-tag candidates.

---

## 1. `unity-preservation` = THE RULE (identity, not analogy)

**Members** (17, 6 categories): see emergence.md for full list.
Notable: `an` (sky=god), `zu` (tooth=know), `ŋeštug` (ear=wisdom),
`ka` (mouth=gate), `im` (wind=rain=cloud=clay), `ud`
(day=sun=light=time), `kur` (mountain=foreign=netherworld).

**Frame mapping**: THE RULE itself — *When two vocabularies
describe the same structural feature, they are describing one
thing, not two analogous things.*

The group IS THE RULE's instantiation in Sumerian vocabulary. Each
of these 17 morphemes carries multiple modern-English concepts in
one sign because the Sumerian speaker saw one substrate coord and
named it once. Modern English split what the Sumerian kept whole.

This is the single sharpest finding of the Sumerian pass.
Sumerian, at the seed level, is ALREADY naming substrate coords
rather than projection-artifact distinctions. THE RULE does not
have to be derived from Sumerian — it is Sumerian's default stance.

---

## 2. `loop-closure` = frame 5 (the loop)

**Members** (4, 3 categories): `im` (1e, hydrological cycle), `iti`
(1e, moon-cycle), `til` (1h, life-walk completion), `gi4` (1i,
return-motion).

**Frame mapping**: frame item 5 — the loop. *Spin = edges = wire =
wave = medium = loop = verbs = function calls = forces = transitions
= reactions = operations = ratios. One thing, many names.*

Four independent vocabulary domains (weather, astronomy, biology,
motion) name closed cycles through four Sumerian morphemes. The
loop operates pervasively; Sumerian speakers named it wherever
they saw it closing. Frame 5 at vocabulary scale.

Also related: `gur` (to turn/reverse) — paired with `gi4` as the
turning-stroke primitive. Together they name the rotation cycle.

---

## 3. Life-trit (frame 2 candidate)

**Members** (3, 1 category, 1h only): `tud` (↑ birth), `til` (↕ live),
`ush` (↓ die).

Sumerian explicitly trit-structures the life-walk with three
separate verbs: `tud` opens, `til` holds, `ush` closes. Same
structural shape as PIE's `bhuH`/`h1es`/`wes` (become/is/was).

**Status**: CANDIDATE. 3 members in 1 category does not pass rule 7
(needs 3+ categories). Needs cross-category reinforcement. Likely
sources:
- Number-trit: diš (1) / min (2) / eš (3) — if the 3-count is polar-
  shaped it joins
- Age-terms: young / middle / old Sumerian equivalents (not yet
  extracted)
- Temporal aspect prefixes in verb-inflection (mu- / ba- / i3-
  patterns)

---

## 4. Transparent compounds = frame 10 emergence (identity)

**Members** (4, 2 categories): `lugal` (3c, king = big-man),
`egal` (3c, palace = big-house), `namlugal` (3c, kingship = role-
of-king), `namtil` (1l, life-as-such = role-of-live).

**Status**: EMERGING (not yet rule-7 solid — needs 3+ categories).

**Interpretation**: Sumerian's transparent composition (`lú` + `gal`
= `lugal` = king) is frame 10 (identity) at the morphological layer.
A Sumerian speaker does not hear "king" as an opaque word; they
hear "big-man" and recognize that the ruler IS the big-man — same
entity, two ways of naming. The composition IS the identity move.
Frame 10 writes the whole vocabulary: many compounds are literally
one-thing-many-names.

---

## Convergence summary (first pass)

| frame item | Sumerian group | members | categories | status |
|---|---|---|---|---|
| THE RULE | unity-preservation | 17 | 6 | SOLID |
| Frame 5 (loop) | loop-closure | 4 | 3 | SOLID |
| Frame 2 (trit) | life-trit | 3 | 1 | CANDIDATE |
| Frame 10 (identity) | transparent-compounds | 4 | 2 | EMERGING |

Two frame items cleanly surface at 46 atomic files (less than 20%
of PIE's graph size). THE RULE fires at the sharpest possible
signal — Sumerian's seeds ARE unity-preservations.

**What this means**: Sumerian independently confirms PIE's
findings. Two unrelated languages, two separate graph-building
efforts, same frame items surface. The substrate asserts itself
through any vocabulary that tries to name the world carefully.
Metrognosis: BLESS.
